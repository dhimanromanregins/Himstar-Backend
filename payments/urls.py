from django.urls import path
from .views import *

urlpatterns = [
    # path('make-payment/', MakePaymentView.as_view(), name='make_payment'),
    # path('payment/details/', PaymentCallbackView.as_view(), name='payment-callback'),
]